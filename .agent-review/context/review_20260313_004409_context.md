# Context Review

## Scope

Review the recent desktop workflow regression fixes and follow-up cleanup/signal restoration work in the Smilegen repo.

Primary goals of the changes:

- restore the visible workspace flow to the canonical five stages: `Import`, `Align`, `Design`, `Review`, `Present`
- prevent users from entering `Present` before review approval
- ensure canonical `design` navigation behaves like legacy `simulate` for photo-in-3D overlay behavior
- restore build/test safety signals that were previously masked

## Changed Areas Under Review

Workflow and navigation:

- `apps/desktop/src/features/views/PresentView.tsx`
- `apps/desktop/src/store/useViewportStore.ts`
- `apps/desktop/src/features/layout/Sidebar.tsx`
- `apps/desktop/src/features/layout/CaseContextBar.tsx`
- `apps/desktop/src/store/useDesignStore.ts`
- `apps/desktop/src/store/useCaseStore.ts`
- `apps/desktop/src/features/views/CaseListView.tsx`
- `apps/desktop/src/features/capture/AlignmentCalibrationWizard.tsx`

Tests and signal restoration:

- `apps/desktop/src/App.test.tsx`
- `apps/desktop/src/features/views/CaptureView.test.tsx`
- `apps/desktop/src/test/setup.ts`
- `apps/desktop/src/test/localStorageIsolation.test.ts`
- `apps/desktop/src/test/viteConfigSignal.test.ts`
- `apps/desktop/vite.config.ts`

## Current Verification Status

Verified locally before this review:

- `pnpm --filter desktop test`
- `pnpm --filter desktop build`

Results:

- desktop tests pass: 35 files, 197 tests
- desktop build passes
- Vite chunk-size warning is visible again for a large `vendor-three-core` chunk
- Node test runner still emits the warning `--localstorage-file was provided without a valid path`; this is considered existing runner-level noise, not a repo code regression

## Requested Review Focus

Prioritize only substantive issues. Ignore style nits.

- logic correctness
- workflow consistency across visible and alias routes
- regression risk from the route/store changes
- test rigor and whether tests prove the behavior they claim
- build/test signal quality

## Constraints

- this is a review, not an implementation task
- prefer concrete findings with file references
- if no issue is found in an area, say so
